#include <stdio.h>

int main()
{

    if (1)
    {
        printf("This if is exicuted!\n");
    }

    if (2345)
    {
        printf("This if is also exicuted\n");
    }

    if (23.45)
    {
        printf("This if is also exicuted\n");
    }

     if ('d')
    {
        printf("This if is also exicuted\n");
    }

    if (0)
    {
        printf("This if is not exicuted");
    }
    return 0;
}
